{
    "name": "setup-terraspace",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "test": "jest",
        "pre-commit": "npm run build && git add dist"
    },
    "devDependencies": {
        "@types/node": "^14.14.37",
        "@vercel/ncc": "^0.28.3",
        "typescript": "^4.9.5"
    },
    "dependencies": {
        "@actions/core": "^1.2.6",
        "@actions/exec": "^1.0.4"
    }
}
